This folder should be kept permanent and never git commit/push
Just drag the files into this folder and keep them
This will allow for us to keep constant string calls to files if we use something like PyCharm 
So we do not need to provide the entire file path and only the local path
